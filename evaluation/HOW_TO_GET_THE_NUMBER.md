Capture the scene on device with `TRACE_EVENTLOG=1` so the session writes a real `events.db`, freeze a copy of `evaluation/gold/real_gold_template.json` into a capture-specific gold file that only asks questions about things that were actually present in that scene, then run `.venv/bin/python evaluation/score_real_capture.py <moment_id-or-events.db> <gold.json>`; the scorer will answer each item through the offline event-log path and write `evaluation/results/real_<moment>.json` plus `evaluation/results/real_<moment>.md` with the correct% and hallucination% 95% Wilson CI report.
