# TRACE iOS App

The production iPhone capture app will live here. The current
`trace-native-fastvlm/` project is a migration input and remains in place until its
capture loop is moved behind the new observation/event-log contracts.

